name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET SDK 8.0
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore ./WebAPI/WebAPI.csproj

    - name: Build
      run: dotnet build --no-restore ./WebAPI/WebAPI.csproj -c Release

    # - name: Publish
    #   run: dotnet publish WebAPI/WebAPI.csproj -c Release -o ./publish /p:UseAppHost=false

    # - name: Log in to Docker Hub
    #   uses: docker/login-action@v2
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}

    # - name: Build Docker image
    #   run: |
    #     docker build -t your-dockerhub-username/moiponewebapi:${{ github.sha }} .

    # - name: Push Docker image
    #   run: |
    #     docker push your-dockerhub-username/moiponewebapi:${{ github.sha }}

    # - name: Deploy to Render via API
    #   env:
    #     RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
    #   run: |
    #     curl -X POST "https://api.render.com/deploy/srv-xxxxxxxxxxxx" \
    #       -H "Authorization: Bearer $RENDER_API_KEY"
